# AIGC 使用记录

## 1. 基本信息

本项目在开发过程中使用 ChatGPT 辅助完成需求分析、环境配置、程序设计、代码调试、测试设计以及 Git/GitHub 使用。

AIGC 主要作为学习和开发辅助工具使用。对于生成的代码和建议，我通过实际运行、人工检查和自动化测试进行了验证。

---

## 2. AIGC 使用过程

### 阶段一：开发环境配置

#### 我的需求

这是我第一次完成此类软件工程作业，电脑中已有 Visual Studio 2022，希望使用 Visual Studio 2022 和 Python 完成“一箭又一箭”小游戏。

#### AIGC 提供的帮助

AIGC 指导我完成了：

- 检查 Visual Studio 2022 的 Python 开发组件；
- 检查 Python 版本；
- 创建 Python 项目；
- 创建 `.venv` 虚拟环境；
- 安装 `pygame-ce`；
- 配置 Visual Studio 使用项目虚拟环境。

#### 遇到的问题

运行 Python 程序时出现：

```text
SyntaxError: Non-UTF-8 code
原因是 Python 文件编码不是 UTF-8。
之后将 Python 文件重新保存为 UTF-8，问题得到解决。
随后又出现：
ModuleNotFoundError: No module named 'pygame'
经过检查发现 pygame 已安装在 .venv 中，但 Visual Studio 使用的是全局 Python。
将项目的 Python 环境切换为：
.venv\Scripts\python.exe
后问题解决。
阶段二：pygame 基础界面
我的需求
希望首先创建一个能够正常启动和关闭的游戏窗口。
AIGC 提供的帮助
AIGC 提供了 pygame 基础程序结构，包括：
- pygame.init()；
- 创建游戏窗口；
- 设置窗口标题；
- 游戏主循环；
- 处理 pygame.QUIT；
- 使用 clock.tick(60) 控制刷新频率。
验证结果
成功创建标题为“一箭又一箭”的 pygame 窗口，并能够通过右上角关闭按钮正常退出。
阶段三：棋盘和箭头表示
我的需求
需要在 pygame 窗口中绘制棋盘，并显示上下左右四种箭头。
AIGC 提供的方案
使用二维列表保存棋盘：
board = [
    [".", ".", "D", "."],
    ["R", ".", ".", "L"],
    [".", "U", ".", "."],
    ["R", ".", ".", "U"]
]
其中：
U = ↑
D = ↓
L = ←
R = →
. = 空格
使用两层 for 循环绘制棋盘，并根据二维列表内容绘制对应箭头。
我的理解
二维列表保存的是游戏真实状态，pygame 只是根据当前数据重新绘制画面。
当一个箭头被消除时，不需要直接“擦除”屏幕上的图案，只需要将对应位置修改为 "."，下一帧重新绘制时箭头自然消失。
阶段四：核心算法 can_exit()
我的需求
判断一个箭头沿其方向一直到棋盘边缘之间是否存在其他箭头。
AIGC 提供的方案
首先定义方向：
DIRECTIONS = {
    "U": (-1, 0),
    "D": (1, 0),
    "L": (0, -1),
    "R": (0, 1)
}
然后从目标箭头前方的第一个格子开始，沿箭头方向逐格扫描。
如果途中发现非空格，则说明箭头被阻挡。
如果一直扫描到棋盘范围之外，则说明箭头可以飞出。
人工验证
例如：
→ . . ↑
虽然箭头右边相邻位置为空，但继续向右扫描后会遇到 ↑，因此 → 不能飞出。
这说明算法不能只检查相邻格，而需要一直检查到棋盘边缘。
阶段五：鼠标操作和游戏状态
我的需求
让玩家能够点击棋盘上的箭头，并实现成功消除、失误次数、游戏失败和通关功能。
AIGC 提供的帮助
使用鼠标坐标：
mouse_x, mouse_y = event.pos
根据棋盘位置和格子大小转换成：
row
col
然后调用：
can_exit(board, row, col)
判断点击结果。
在后续开发中加入了：
- 3 次失误机会；
- 重新开始；
- 返回菜单；
- 本关通过；
- 下一关；
- 游戏失败；
- 全部通关；
- 三个游戏关卡。
阶段六：关卡可解性检查
我的需求
避免人工设计出无法通关的关卡。
AIGC 提供的方案
编写：
is_solvable()
自动尝试不断消除当前能够飞出的箭头。
如果最终可以将所有箭头消除，则关卡可解。
如果棋盘还存在箭头，但已经没有任何箭头能够消除，则说明出现死锁，关卡不可解。
验证结果
程序启动时会自动检查三个正式关卡。
如果发现不可解关卡，会抛出错误提醒修改关卡。
阶段七：自动化测试
我的需求
学习如何使用 pytest 对游戏核心逻辑进行测试。
AIGC 提供的帮助
将游戏逻辑从 pygame 主程序中分离到：
game_logic.py
然后建立：
test_game_logic.py
测试内容包括：
1. 前方无遮挡箭头能够飞出；
2. 前方存在箭头时不能飞出；
3. 边界箭头；
4. 空格不能作为箭头；
5. 清空棋盘后的通关判断；
6. 未清空棋盘判断；
7. 剩余箭头数量统计；
8. 关卡可解性检查。
最终运行：
python -m pytest -v
得到：
8 passed
阶段八：Git 与 GitHub
我的需求
这是我第一次使用 Git 和 GitHub 管理软件工程项目。
AIGC 提供的帮助
AIGC 指导完成：
- 创建 .gitignore；
- 使用 git init；
- 使用 git status 检查项目；
- 使用 git add；
- 设置 Git 用户信息；
- 使用 git commit；
- 创建 GitHub 公共仓库；
- 添加远程仓库；
- 将默认分支调整为 main；
- 使用 git push 上传项目；
- 编写并提交 README。
3. AIGC 结果的验证方式
为了避免直接使用错误的 AIGC 输出，本项目采用了以下方式验证：
1. 每增加一个功能后实际运行程序；
2. 通过人工操作检查成功消除和错误点击；
3. 使用 pytest 自动测试核心算法；
4. 使用 is_solvable() 自动检查关卡可解性；
5. 使用 Git 保存关键版本；
6. 对报错信息进行分析后再修改代码。
4. AIGC 使用心得
本次作业是我第一次完整开发此类小游戏，同时也是第一次系统使用 pygame、pytest、Git 和 GitHub。
AIGC 最大的帮助并不是简单生成完整代码，而是在开发过程中将一个较大的任务拆分成多个可以逐步完成的小任务。
例如，一开始并没有直接完成整个游戏，而是按照：
Python 环境
→ pygame 窗口
→ 棋盘
→ 箭头
→ can_exit()
→ 鼠标点击
→ 游戏状态
→ 多关卡
→ 自动化测试
→ Git
→ GitHub
逐步完成。
在开发过程中也发现，AIGC 生成的内容不能直接认为一定正确。例如文件编码、Python 环境、代码括号错误以及 Git 配置等问题，都需要结合实际运行结果继续判断和修改。
因此，我认为使用 AIGC 时比较重要的是理解关键代码、实际运行验证结果，并能够解释程序为什么这样设计。
```text